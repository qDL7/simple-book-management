{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
    <div class="row mt-2">
        <div class="col">
            <h1>Edit Book</h1>
        </div>
    </div>
    <div class="row mt-2">
        {{ include('_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <div class="row mt-2">
        <a href="{{ path('book_list') }}">
            <button class="btn btn-secondary">Back to list</button>
        </a>
        {{ include('_delete_form.html.twig', {'id': id, 'delete_route_name': 'book_delete'}) }}
    </div>
{% endblock %}
